on:
  workflow_dispatch:
 
jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a PowerShell command
        run: Write-Output "Hello from Windows VM!"
